import { Component } from '@angular/core';

/**
 * `FooterComponent` is a component that provides the footer.
*/
@Component({
  selector: 'app-footer',
  templateUrl: './footer.component.html',
  styleUrls: ['./footer.component.scss']
})

export class FooterComponent {
}
